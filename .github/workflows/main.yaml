name: testing
on:
  - push
jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/workflows/action
      - uses: actions/upload-artifact@v3
        with:
          name: test-file
          path: $GITHUB_WORKSPACE/testing
